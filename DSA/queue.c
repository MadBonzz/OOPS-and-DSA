#include<stdio.h>
#include<stdlib.h>

struct Node{
    int size;
    int front;
    int rear;
    int * arr;
};

int main(){
    struct Node * queue; 
    printf("Enter the size of the queue : ");
    scanf("%d", &queue->size);
    
}